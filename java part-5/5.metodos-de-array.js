/* 
    Array's
        - push   -> Adiciona mais um ao array
        - lenght -> Mostra o tamanho do array
        - sort   -> Organiza o array
        - delete -> Deleta o item de uma posição do Array
        - every  -> Esse método testa se todos os elementos do Array passam em uma condiçâo. P
        - some   -> O .some() faz algo parecido com o .every(). A diferença é que o .every() s
        - fill   -> Preenche posições do array como desejarmos
        - find   -> Usamos esse método quando queremos encontrar algum elemento dentro no Array
        - findIndex -> o mesmo que o .find() mas retorna o índice do elemento encontrado ao
        - concat()  -> 


        - includes  -> O método includes() determina se um array contém um determinado elemento
        - join -> O método join() junta todos os elementos de um array

        - slice(fatiar)  ->
        - splice ->
        - pop    ->
        - shift  ->

*/

// const numbers = [1, 2, 3, 4]

// numbers.fill("abacate", 0, 2)

// console.log(numbers)


// const array1 = ['a', 'b', 'c'];
// const array2 = ['d', 'e', 'f'];
// const numbers1 = [1, 2, 5]
// const numbers2 = [2, 4, 6]

// const array3 = array1.concat(array2)
// console.log(array3)






// const elements = ['Fire', 'Air', 'water'];

// elements.shift()

// console.log(elements)



const users = ["João", "Maria", "Pedro", "Michele", "Fernado"];

users.splice(0, 3, "patrícia")

console.log(users)
